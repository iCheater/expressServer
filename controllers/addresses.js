const appRoot = require('app-root-path')
const logger = require(`${appRoot}/helpers/winstonLogger`)
const { Address, Token } = require(`${appRoot}/models`)


exports.create = (req, res) => {
  //
  // res.send('NOT IMPLEMENTED: Author list')
}
